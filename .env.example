# Frontend (Next.js) env — copy to .env.local. Only PUBLIC values here; never secrets.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# FastAPI backend base URL (e.g. http://localhost:8000 in dev, Railway URL in prod)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
